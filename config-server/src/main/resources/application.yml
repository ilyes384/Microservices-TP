server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ilyes384/microservice-spring-cloud.git
          clone-on-start: true
          search-paths: configs
    compatibility-verifier:
      enabled: false
